# Bangumi API Cache

Machine-managed incremental API payloads from `https://github.com/bangumi/api` belong here.

The provider package supports incremental updates for known Bangumi subject IDs through the `/v0/subjects/{id}` API shape, then feeds responses through the same normalizer used by Archive imports.

Do not edit these files manually.
