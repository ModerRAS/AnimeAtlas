# Normalized Database

Provider-neutral normalized records live here.

This early tranche uses one bootstrap record so validation and generated indexes have a concrete fixture. Long term, records in this directory are produced from provider inputs plus approved source records.
